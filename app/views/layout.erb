<!doctype html>
<html>
  <head>
    <title>Fwitter</title>
  </head>
  <body>
    <div align="right" id="links">
      <%if logged_in?%>
        Welcome, <%=current_user.username%>!<br>
        <a href="/tweets/new">Create new Tweet!</a> <br>
        <a href="/users/<%=current_user.slug%>">Your Profile</a> <br>
        <a href="/tweets">Home</a> <br><br>
        <a href="/logout">Log Out</a>
      <%else%>
        <a href="/login">Login</a> <br>
        <a href="/signup">Sign Up</a>
      <%end%>
    </div>

    <div class="container">
      <%=yield%>
    </div>

  </body>
</html>
